// #14 Arrow Function

// 1. Recreate the last assignment, but this time create an arrow function called 'percentageOfWorld3'

const percentageOfWorld3 = (population) => (population / 7900) * 100;

const popSouthKorea3 = percentageOfWorld3(52);
const popUnitedStates3 = percentageOfWorld3(330);
const popFrance3 = percentageOfWorld3(67);
console.log(popSouthKorea3, popUnitedStates3, popFrance3);
